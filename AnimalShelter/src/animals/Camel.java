package animals;
public class Camel extends PackAnimals {
    public Camel(String name, String birthDate, String[] commands, int loadCapacity) {
        super(name, birthDate, commands, loadCapacity);
    }
}
